{
  "mcpServers": {
    "business-central": {
      "type": "http",
      "url": "https://mcp.businesscentral.dynamics.com",
      "headers": {
        "TenantId": "${BC_TENANT_ID}",
        "EnvironmentName": "${BC_ENVIRONMENT_NAME}",
        "Company": "${BC_COMPANY}",
        "ConfigurationName": "${BC_MCP_CONFIGURATION}"
      }
    }
  }
}
